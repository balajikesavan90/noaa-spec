# Examples

Optional example surface. Not part of reviewer quickstart.

Run the installed-package example:

```bash
python examples/run_minimal_cleaning.py --out /tmp/noaa-spec-example.csv
```

For the canonical reviewer path, start with the root `README.md`.
