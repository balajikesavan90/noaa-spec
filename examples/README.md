# Examples

Non-canonical example surface. Not part of reviewer quickstart.

Run the minimal installed-package example:

```bash
poetry run python3 examples/run_minimal_cleaning.py --out /tmp/noaa-spec-example.csv
```

This uses the tracked reproducibility sample input and exercises the `noaa_spec` package directly.

For the canonical reviewer path, start with the root `README.md`.
