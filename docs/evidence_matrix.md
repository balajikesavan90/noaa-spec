# Evidence Matrix

This matrix maps the core NOAA-Spec reviewer-facing claims to exact repository evidence. It is intended to make the JOSS evidence boundary auditable without expanding the paper.

Evidence strength is labeled conservatively:

- `upstream-traceable fixture-backed`: the evidence has a committed raw/expected output pair plus recorded NOAA/NCEI source URL, retrieval date, and upstream checksum.
- `fixture-backed`: the evidence has committed raw/expected output pairs verified by `reproducibility/checksums.sha256`, but does not necessarily replay upstream NOAA retrieval.
- `unit-test-backed`: the behavior is covered by regression tests, but the small fixtures do not demonstrate the full supported surface.
- `documentation-only`: the item is a scope or boundary statement, not an executable evidence claim by itself.

The reproducibility boundary is `clean(committed_input) = committed_output` for the public `noaa-spec clean` CLI. Only `reproducibility/real_provenance_example/` adds upstream NOAA URL/date/checksum traceability. The curated example set in `artifacts/curated_examples/` improves auditability of mined real-row edge cases with NOAA station-year source URLs rather than a full upstream retrieval replay.

| Core claim | Evidence type | Supporting fixtures / artifacts | Supporting tests | Supporting docs | Notes |
| --- | --- | --- | --- | --- | --- |
| Deterministic cleaned CSV generation from the public CLI | fixture-backed; unit-test-backed | `reproducibility/*/station_raw.csv`; `reproducibility/*/station_cleaned_expected.csv`; `reproducibility/checksums.sha256` | `tests/test_reproducibility_example.py::test_reproducibility_fixture_output_matches_expected`; `tests/test_cli.py::test_cli_clean_writes_canonical_csv`; `tests/test_deterministic_io.py` | `REPRODUCIBILITY.md`; `README.md`; `docs/schema.md` | Strong for committed fixtures. Not a claim that NOAA upstream retrieval is replayed for every fixture. |
| Upstream-traceable real-data example | upstream-traceable fixture-backed | `reproducibility/real_provenance_example/station_raw.csv`; `reproducibility/real_provenance_example/station_cleaned_expected.csv`; upstream checksum entry in `reproducibility/checksums.sha256` | `tests/test_reproducibility_example.py::test_reproducibility_fixture_output_matches_expected` | `reproducibility/REAL_PROVENANCE_EXAMPLE.md`; `REPRODUCIBILITY.md` | Strongest provenance evidence, but intentionally one 20-row source slice only. |
| Sentinel-to-null normalization for supported fields | fixture-backed; unit-test-backed | Tracked expected outputs under `reproducibility/`; curated edge cases in `artifacts/curated_examples/curated_examples.csv` | `tests/test_cli.py::test_cli_clean_preserves_quality_code_when_sentinel_is_null`; `tests/test_cleaning.py::TestSentinelsInCleanedOutput`; `tests/test_cleaning.py::TestIsMissingValue` | `docs/supported_fields.md`; `docs/rule_provenance.md`; `docs/reviewer_cleaning_examples.md` | Strong for fixture-demonstrated cases such as mandatory fields; broader family behavior is test-backed rather than exhaustively fixture-backed. |
| NOAA QC/context preservation when values are nullified | fixture-backed; unit-test-backed | Tracked expected outputs under `reproducibility/`; curated examples with `estimated_qc_preserved`, calm wind, and AA1 partial-sentinel rows in `artifacts/curated_examples/` | `tests/test_cli.py::test_cli_clean_preserves_quality_code_when_sentinel_is_null`; `tests/test_cleaning.py::TestQualityForPart`; `tests/test_cleaning.py::TestQualityNullsCorrectPart`; `tests/test_qc_comprehensive.py` | `docs/supported_fields.md`; `docs/schema.md`; `docs/rule_provenance.md` | Evidence supports preserved QC columns and parser sidecars; it does not imply every NOAA QC code is scientifically reclassified. |
| Stable, explicit decoded column naming for the supported release surface | fixture-backed; unit-test-backed | Tracked cleaned expected CSV headers under `reproducibility/` | `tests/test_cleaning.py::TestPrefixRuleMapping`; `tests/test_cleaning.py::TestTwoPartFieldNamingAndQuality`; `tests/test_cli.py::test_cli_clean_writes_canonical_csv` | `docs/supported_fields.md`; `docs/schema.md`; `README.md` | The output is deterministic for a given input, but the emitted column set depends on which supported NOAA fields are present. |
| Checksum-stable reproduction of tracked fixtures | fixture-backed | `reproducibility/checksums.sha256`; `scripts/verify_reproducibility.sh`; all tracked raw/expected fixture pairs | `tests/test_reproducibility_example.py::test_reproducibility_fixture_output_matches_expected` | `REPRODUCIBILITY.md`; `reproducibility/README.md`; `reproducibility/FIXTURE_PROVENANCE.md` | Strong for repository-committed input/output pairs. The Docker reviewer path is tested, not an immutable archived runtime. |
| Bounded supported field-family coverage | unit-test-backed; fixture-backed for selected families | Tracked fixtures demonstrate selected mandatory and additional families; `artifacts/curated_examples/` demonstrates mined `TMP`, `VIS`, `WND`, and `AA1` edge cases | `tests/test_cleaning.py`; `tests/test_qc_comprehensive.py`; `tests/test_reproducibility_example.py` | `docs/supported_fields.md`; `docs/rule_provenance.md`; `docs/first_output_guide.md` | The registry is the coverage contract. The curated mined examples do not cover pressure, cloud, remarks, or every supported family because the mined pool used for this selection contains only `TMP`, `VIS`, `WND`, and `AA1`. |
| Curated high-value real-row edge cases | documentation-only | `artifacts/curated_examples/curated_examples.csv`; `artifacts/curated_examples/curated_examples.md`; `artifacts/curated_examples/selection_summary.md`; generator `tools/select_curated_examples.py` | None specific to the curated artifact | `docs/reviewer_cleaning_examples.md`; this matrix | These are selected from the existing example pool with NOAA station-year source URLs derived from station ID and observation year. They improve auditability, but are not upstream-traceable fixtures unless separately promoted into `reproducibility/`. |
| Reproducibility boundary | documentation-only | `reproducibility/checksums.sha256`; fixture directories listed in `REPRODUCIBILITY.md` | `tests/test_reproducibility_example.py` verifies the executable part | `REPRODUCIBILITY.md`; `reproducibility/REAL_PROVENANCE_EXAMPLE.md`; `reproducibility/FIXTURE_PROVENANCE.md` | The repository claims deterministic cleaning for committed fixtures, one upstream-traceable source slice, and tested parser behavior. It does not claim full NOAA downloading or exhaustive NOAA coverage. |
| Optional `split-domains` convenience behavior is outside the core JOSS claim | unit-test-backed; documentation-only | Optional runtime outputs generated by `noaa-spec split-domains`, not part of `reproducibility/checksums.sha256` | `tests/test_cli.py::test_cli_split_domains_writes_optional_domain_csvs`; `tests/test_domain_split.py` | `README.md`; `REPRODUCIBILITY.md`; `docs/supported_fields.md` | Tests show the command still works, but it is explicitly outside the core paper/reviewer reproducibility claim. |

## Honest Gaps

- Only `reproducibility/real_provenance_example/` is upstream-traceable to a NOAA URL, retrieval date, and upstream checksum.
- The curated example set records NOAA station-year source URLs, but exact row retrieval has not been independently replayed as a committed reproducibility fixture.
- The mined example pool used here covers `TMP`, `VIS`, `WND`, and `AA1`; other supported families rely on tracked fixtures where present and unit tests rather than this curated example artifact.
- NOAA-Spec does not claim exhaustive coverage for all NOAA ISD / Global Hourly identifiers or every possible value within supported families.
