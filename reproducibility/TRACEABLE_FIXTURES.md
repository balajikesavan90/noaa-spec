# Traceable Reproducibility Fixtures

This document records the upstream-traceable real-world fixtures in the repository. They are intentionally small: they give reviewers NOAA/NCEI source URLs, retrieval dates, observed upstream checksums, and exact extraction commands for selected `noaa-spec clean` slices. They do not turn NOAA-Spec into a downloader or station-processing workflow.

## Fixture Summary

| Fixture | Station / year | Rows | Selection | Field-family evidence added |
| --- | --- | --- | --- | --- |
| `real_provenance_example/` | `78724099999` / 2001 | 20 | Header plus first 20 data rows | Mandatory `WND`, `CIG`, `VIS`, `TMP`, `DEW`, `SLP`; selected `AA1`, `GA`, `GF`, `IA1`, `KA`, `MA`, `MD`, `MW`, `REM`, `EQD` fields where present. |
| `traceable_peru_il_2014_aa1_qc/` | `72214904899` / 2014 | 1 | Header plus NOAA CSV line 3616 | `AA1` amount sentinel with QC/context preservation, `GA`/`GD` cloud fields, `KA` temperature summary fields, `MA` pressure, `OC1` gust, `REM`. |
| `traceable_albion_ne_2014_calm_aa1/` | `72344154921` / 2014 | 1 | Header plus NOAA CSV line 15253 | Calm-wind `WND` context, `AA1` amount sentinel with QC/context preservation, `GA`/`GD`/`GE`/`GF` cloud fields, `KA` temperature summary fields, `MA` pressure, `REM`, `EQD`. |

## Source Metadata

| Fixture | Station name | NOAA source URL | Retrieval date | HTTP `Last-Modified` observed | Upstream source CSV checksum observed |
| --- | --- | --- | --- | --- | --- |
| `real_provenance_example/` | Choluteca, Honduras | `https://www.ncei.noaa.gov/data/global-hourly/access/2001/78724099999.csv` | 2026-04-12 | 2020-03-17 13:48:14 GMT | `18eee5a542603cd803e9ebe49de467c2f68f3e510ef1a781d427c50e86ac9214` |
| `traceable_peru_il_2014_aa1_qc/` | Peru IL Valley Regional Walter A Duncan Field Airport, Illinois, US | `https://www.ncei.noaa.gov/data/global-hourly/access/2014/72214904899.csv` | 2026-04-13 | 2021-02-04 21:38:08 GMT | `24b77624cc8746cb836303d793d44117c58f282cbee28ba5ba44eb7ea56beaa3` |
| `traceable_albion_ne_2014_calm_aa1/` | Albion Municipal Airport, Nebraska, US | `https://www.ncei.noaa.gov/data/global-hourly/access/2014/72344154921.csv` | 2026-04-13 | 2021-02-04 21:41:36 GMT | `8ea86e2f3a7133d426db03d19a560d4ac14f385d3d0158fbf8a58180f0450e25` |

The fixture raw and expected-cleaned CSV checksums are recorded in `reproducibility/checksums.sha256`.

## Exact Extraction Commands

The existing 20-row example is a mechanical head slice:

```bash
curl -fsSL https://www.ncei.noaa.gov/data/global-hourly/access/2001/78724099999.csv \
  | head -n 21 \
  > reproducibility/real_provenance_example/station_raw.csv
```

The two additional one-row fixtures promote already documented reviewer edge cases into upstream-traceable committed fixtures:

```bash
curl -fsSL -o /tmp/noaa_2014_72214904899.csv \
  https://www.ncei.noaa.gov/data/global-hourly/access/2014/72214904899.csv
sha256sum /tmp/noaa_2014_72214904899.csv
sed -n '1p;3616p' /tmp/noaa_2014_72214904899.csv \
  > reproducibility/traceable_peru_il_2014_aa1_qc/station_raw.csv

curl -fsSL -o /tmp/noaa_2014_72344154921.csv \
  https://www.ncei.noaa.gov/data/global-hourly/access/2014/72344154921.csv
sha256sum /tmp/noaa_2014_72344154921.csv
sed -n '1p;15253p' /tmp/noaa_2014_72344154921.csv \
  > reproducibility/traceable_albion_ne_2014_calm_aa1/station_raw.csv
```

Expected cleaned outputs are generated by the public CLI:

```bash
noaa-spec clean \
  reproducibility/real_provenance_example/station_raw.csv \
  reproducibility/real_provenance_example/station_cleaned_expected.csv

noaa-spec clean \
  reproducibility/traceable_peru_il_2014_aa1_qc/station_raw.csv \
  reproducibility/traceable_peru_il_2014_aa1_qc/station_cleaned_expected.csv

noaa-spec clean \
  reproducibility/traceable_albion_ne_2014_calm_aa1/station_raw.csv \
  reproducibility/traceable_albion_ne_2014_calm_aa1/station_cleaned_expected.csv
```

The same fixtures are included in `scripts/verify_reproducibility.sh`, which regenerates cleaned output in a temporary directory and checks it against `reproducibility/checksums.sha256`.

## Limitations

- NOAA-Spec does not download NOAA data; source URLs, retrieval dates, and observed upstream checksums are recorded so reviewers can independently inspect the source files used for these examples.
- The upstream NOAA URLs are external services. The recorded upstream checksums are evidence for the files as observed on the retrieval dates above, not a guarantee that NOAA will never revise or move them.
- These fixtures demonstrate selected traceable real-world source slices. They do not claim exhaustive NOAA field coverage or equal real-data evidence for every implemented field family.
